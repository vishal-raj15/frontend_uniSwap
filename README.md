# frontend_uniSwap

it is a clone of uniswap with functionality of AMM

clone the repo

`cd frontend_uniswap`
`yarn install`
`cd UniClone`
`npx hardhat node`
`npx hardhat compile`
`npx hardhat deploy --network localhost`
